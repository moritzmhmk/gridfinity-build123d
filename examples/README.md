# Examples

Before executing the examples start ocp_vscode and open the viewer in a Browser (or use the VS Code extension):
```shell
uv run -m ocp_vscode
```

To use the local gridfinity package:
```shell
uv pip install -e .
```
